CBR integrates an existing simple task ____ book purchase block chain. It utilises a simple go code to generate a blockchain along with the EnvFactor which we have included in every block. It generates carbon emission
.
